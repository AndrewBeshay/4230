function coordinates = Main4_MatchBlocks(shapeProps, patternShapeProps)

desiredIdx = find(patternShapeProps(:,1) == shapeProps(1) & patternShapeProps(:,2) == shapeProps(2));
if length(desiredIdx) > 1
    coordinates = patternShapeProps(desiredIdx, 3:6);
else
    coordinates = [];
end

end